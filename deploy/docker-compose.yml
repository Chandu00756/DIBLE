services:
  dible-control-plane:
    build: ..
    ports: ["8080:8080"]
    environment:
      DIBLE_DB_PATH: /var/lib/dible/dible.db
    volumes:
      - dible_data:/var/lib/dible
    security_opt: ["no-new-privileges:true"]
    restart: unless-stopped
volumes:
  dible_data:
